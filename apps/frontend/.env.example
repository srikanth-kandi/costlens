# Copy this file to .env and fill in the values.
#
# In local dev this can be left as /api because Vite's dev proxy
# forwards /api → http://localhost:3001.
#
# On Render (or any static host) set this to your full backend URL,
# e.g. https://costlens-backend.onrender.com/api
VITE_API_BASE_URL=/api
